# dependencies / build
node_modules/
*.log

# OS junk
.DS_Store
**/.DS_Store

# runtime / user data (do NOT commit — contains visitor IPs / uploads / deleted backups)
admin-data/events.ndjson
assets/admin-uploads/*
!assets/admin-uploads/.gitkeep
_deleted_products/
